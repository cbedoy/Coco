<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>

    <stroke android:color="@color/dividerColor"
        android:width="1dp"/>

    <corners
        android:bottomLeftRadius="@dimen/common_size"
        android:bottomRightRadius="@dimen/common_size"
        android:topLeftRadius="@dimen/common_size"
        android:topRightRadius="@dimen/common_size"/>

    <padding android:left="@dimen/small_size"
        android:top="@dimen/small_size"
        android:right="@dimen/small_size"
        android:bottom="@dimen/small_size" />
</shape>